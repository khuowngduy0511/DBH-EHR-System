$kind: http-request
url: "{{ehr_url}}/Ehr/records/{{ehr_id}}/document"
method: GET
headers:
  X-Requester-Id: "{{doctor_id}}"
auth:
  type: bearer
  credentials:
    token: "{{doctor_token}}"
order: 4000
