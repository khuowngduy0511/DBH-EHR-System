$kind: http-request
url: "{{baseUrl}}/api/v1/ehr/records/{{ehrId}}"
method: PUT
body:
  type: json
  content: |-
    {
      "data": {
        "diagnosis": "Viem phoi - J18.9 (Updated)",
        "treatment": "Amoxicillin 500mg x 3 lan/ngay + Paracetamol 500mg",
        "vitals": {
          "temperature": 37.0,
          "blood_pressure": "120/80"
        },
        "follow_up": "Tai kham sau 7 ngay"
      }
    }
auth:
  type: bearer
  credentials:
    token: "{{doctorToken}}"
order: 6000
