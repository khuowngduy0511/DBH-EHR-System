$kind: http-request
url: "{{baseUrl}}/api/v1/audit/stats"
method: GET
queryParams:
  - key: organizationId
    value: "{{orgId}}"
    disabled: true
  - key: fromDate
    value: ""
    disabled: true
  - key: toDate
    value: ""
    disabled: true
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
order: 7000
