$kind: http-request
url: "{{baseUrl}}/api/v1/organizations/{{orgId}}"
method: PUT
body:
  type: json
  content: |-
    {
      "orgName": "Benh Vien Da Khoa DBH (Updated)",
      "website": "https://dbh-updated.vn"
    }
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
order: 4000
