$kind: http-request
url: "{{baseUrl}}/api/v1/consents/{{consentId}}/revoke"
method: POST
body:
  type: json
  content: |-
    {
      "revokeReason": "Khong muon chia se nua"
    }
auth:
  type: bearer
  credentials:
    token: "{{patientToken}}"
order: 7000
