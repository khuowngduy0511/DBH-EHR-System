$kind: http-request
url: "{{baseUrl}}/api/v1/patients/{{patientId}}"
method: PUT
body:
  type: json
  content: |-
    {
      "dob": "1990-06-20",
      "bloodType": "A+"
    }
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
order: 4000
