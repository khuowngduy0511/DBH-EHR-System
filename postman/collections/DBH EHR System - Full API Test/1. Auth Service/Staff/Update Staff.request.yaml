$kind: http-request
url: "{{baseUrl}}/api/v1/staff/{{staffId}}"
method: PUT
body:
  type: json
  content: |-
    {
      "role": "Pharmacist",
      "licenseNumber": "DD-2024-002",
      "verifiedStatus": "Verified"
    }
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
order: 4000
