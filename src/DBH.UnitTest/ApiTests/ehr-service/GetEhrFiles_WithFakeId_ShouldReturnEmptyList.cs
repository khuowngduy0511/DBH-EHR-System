using System.Net;using System.Net.Http.Json;using System.Text.Json;using DBH.UnitTest.Shared;namespace DBH.UnitTest.ApiTests;public class EhrServiceTests_GetEhrFiles_WithFakeId_ShouldReturnEmptyList : ApiTestBase{    protected override IReadOnlyCollection<string> RequiredServices => new[]    {    "AuthService",    "EhrService"    };    [SkippableFact]    public async Task GetEhrFiles_WithFakeId_ShouldReturnEmptyList()    {    await AuthenticateAsAdminAsync(EhrClient);    var response = await GetWithRetryAsync(EhrClient, ApiEndpoints.Ehr.Files(Guid.NewGuid()));        Assert.Equal(HttpStatusCode.OK, response.StatusCode);    }}
