using System.Net;using System.Net.Http.Json;using System.Text.Json;using DBH.UnitTest.Shared;namespace DBH.UnitTest.UnitTests;public class EhrServiceTests_GetOrgEhrRecords_WithSeedOrg_ShouldReturnResult : ApiTestBase{    protected override IReadOnlyCollection<string> RequiredServices => new[]    {    "AuthService",    "EhrService"    };    [SkippableFact]    public async Task GetOrgEhrRecords_WithSeedOrg_ShouldReturnResult()    {    await AuthenticateAsAdminAsync(EhrClient);    var response = await GetWithRetryAsync(EhrClient, ApiEndpoints.Ehr.OrgRecords(TestSeedData.HospitalAOrgId));        Assert.Equal(HttpStatusCode.OK, response.StatusCode);    var json = await ReadJsonResponseAsync(response);    Assert.Equal(JsonValueKind.Array, json.ValueKind);    }}